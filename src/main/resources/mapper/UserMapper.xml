<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="week01.project.mapper.UserMapper">

    <resultMap id="UserMap" type="week01.project.pojo.User">
        <id property="id" column="id"/>
        <result property="role" column="role"/>
        <result property="name" column="name"/>
        <result property="password" column="password"/>
    </resultMap>

    <select id="selectById" resultMap="UserMap">
        select id, name, password, role
        from user
        where id = #{id}
    </select>

    <insert id="insertUser" parameterType="week01.project.pojo.User" keyProperty="id">
        insert into user(id, password, name, role)
        values (#{id}, #{password}, #{name}, #{role})
    </insert>

    <delete id="deleteById" parameterType="String">
        delete
        from user
        where id = #{id}
    </delete>
</mapper>
